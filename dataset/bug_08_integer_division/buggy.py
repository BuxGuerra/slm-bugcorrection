def split_evenly(total, people):
    """Splits `total` items evenly among `people` people and returns
    how many whole items each person receives (discarding the remainder)."""
    return total / people
